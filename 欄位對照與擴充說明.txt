目前版本使用單一共用工序工時表。

若未來要依 Type、P/N 或 Category 使用不同工時，可在「工時設定」增加：
- Type
- P/N
- Category

再於 scheduler.py 的 build_schedule() 中，依訂單列篩選對應工時。
本套件已將排程核心獨立在 scheduler.py，方便後續擴充：
- 依人員產能排程
- 同時在線上限
- 急單優先權
- 機台或場地限制
- 製令相依關係
- 甘特圖輸出
